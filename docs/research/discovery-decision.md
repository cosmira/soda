# Decision frozen before held-out inspection

Discovery inspection: 70 selected findings and 15 non-findings, recorded in JSON alongside this file. One assistant inspected source; no independent reviewer panel or comprehension experiment took place.

- Keep cognitive complexity as an explicitly optional review signal (15). Several findings suggest local simplification, but option tables, guards and language formulas can be legitimate. Do not add it to the 69-rule standard set.
- Reject the disconnected-method-groups threshold as a built-in rule. Keep the graph and TCC as research facts, and move the candidate check into the reproducible research harness. The observed groups repeatedly penalize fluent state, concerns and protocol defaults. Do not repair that by excluding framework names or adding arbitrary quotas.
- Keep the two exception checks opt-in. Two discovery examples are too few to establish broad usefulness.
- Fix the observed implicit `(string) $this` edge to `__toString` and mark iteration/clone of this unresolved before held-out collection. This is a semantic collector correction, not threshold tuning. Preserve the initial discovery selection and judgments; rerun discovery measurements separately.
- No standard thresholds, allowed-violation budgets, config syntax or dependencies change.

The held-out Symfony Console and Guzzle source/results had not been inspected when this decision was recorded. The corpus is framework/library-heavy and does not represent an independent business application.
